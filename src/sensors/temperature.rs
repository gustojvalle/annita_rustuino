use anyhow::Result;

fn convert_volt_to_temperature(adc_value: u16) -> f32 {
    // TODO calculate temperature
    return 0.0;
}

pub fn read_temperature() -> Result<f32> {
    // TODO take average temperature between sensor probes.
    return Ok(0.0);
}
